package main;

public class Main {

    static boolean[] CMV = new boolean[15];
    static boolean[] PUV = new boolean[15];
    static boolean[] FUV = new boolean[15];
    static int[][] PUM = new int[15][15];


    static int NUM_POINTS = 100;
    static Point[] POINTS = new Point[NUM_POINTS];


    public static void decide(){
        // CMV[i] = LIC_i()
        // CMV[i] = LIC_i()
        // CMV[i] = LIC_i()
        // CMV[i] = LIC_i()
        // CMV[i] = LIC_i()
        // CMV[i] = LIC_i()
        // CMV[i] = LIC_i()
        // CMV[i] = LIC_i()
        // CMV[i] = LIC_i()
        // CMV[i] = LIC_i()
        // CMV[i] = LIC_i()
        // CMV[i] = LIC_i()
        // CMV[i] = LIC_i()
        // CMV[i] = LIC_i()
        // CMV[i] = LIC_i()


    }




    public static void main(String[] args) {

    }
}
